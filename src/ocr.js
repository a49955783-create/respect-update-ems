export async function ocrExtract(fileOrUrl){ const { Tesseract } = await import('tesseract.js'); const res = await Tesseract.recognize(fileOrUrl, 'ara+eng'); const lines = (res?.data?.lines||[]).map(l=>l.text.trim()).filter(Boolean); return lines } export function parseNameCode(line){ return {name: line, code: ''} }